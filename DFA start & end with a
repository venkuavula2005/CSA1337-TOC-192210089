#include<stdio.h>
#include<string.h>

int main() {
    char s[100];
    scanf("%s", s);
    printf("%s\n", (s[0] == 'a' && s[strlen(s)-1] == 'a') ? "String accepted" : "String rejected");
    return 0;
}
